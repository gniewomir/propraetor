# Domain front for __FQDN__ — Edge-owned (ADR-0028). Reconciled by ensure-components.
server {
    listen 80;
    listen [::]:80;
    server_name __FQDN__;

    location ^~ /.well-known/acme-challenge/ {
        root /var/www/acme;
        default_type text/plain;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}

server {
    listen 443 ssl;
    listen [::]:443 ssl;
    server_name __FQDN__;

    ssl_certificate     /etc/nginx/certs/__FQDN__/fullchain.pem;
    ssl_certificate_key /etc/nginx/certs/__FQDN__/privkey.pem;

    location = /healthcheck {
        default_type text/plain;
        return 200 'ok';
    }

    include /etc/nginx/edge-routes/*--__FQDN__.conf;
}
